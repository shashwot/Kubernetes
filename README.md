```[KUBERNETES]```

Then following files contains the installation to a kubernetes cluster in localmachine

1. centos_kubernetes.sh
    - Install this in your centos machine
2. cluster_kind.yaml
    - This contains the yaml file to create a kubernetes cluster using kind.
3. cluster_ctlptl.yaml
    - This contains the yaml file to create a kibernetes cluster using ctlptl.
